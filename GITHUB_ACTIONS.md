# 🚀 GitHub Actions - Автоматическая сборка

## Что создано

✅ **3 workflow файла** для автоматической сборки:
- `build-macos-portable.yml` - сборка для macOS
- `build-windows-portable.yml` - сборка для Windows  
- `build-all-platforms.yml` - сборка для всех платформ одновременно

## Как использовать

### Вариант 1: Ручной запуск

1. Перейдите в ваш GitHub репозиторий
2. Откройте вкладку **Actions**
3. Выберите нужный workflow (например, "Build macOS Portable")
4. Нажмите **Run workflow**
5. (Опционально) Укажите версию
6. Нажмите **Run workflow**

Через несколько минут сборка будет готова! Скачайте артефакт из раздела **Actions**.

### Вариант 2: Автоматический запуск через тег

```bash
# Создайте тег версии
git tag v1.0.0

# Запушьте тег
git push origin v1.0.0
```

Workflow автоматически:
- ✅ Запустится
- ✅ Соберет портативную версию
- ✅ Создаст релиз в GitHub
- ✅ Прикрепит ZIP архив к релизу

### Вариант 3: Сборка всех платформ

Используйте `build-all-platforms.yml` для одновременной сборки Windows и macOS версий.

## Что включают портативные версии

✅ **Electron приложение** - готовое к запуску  
✅ **Встроенный Python 3.11** - виртуальное окружение со всеми зависимостями  
✅ **Все Python пакеты** - предустановлены (FastAPI, uvicorn, lxml, pandas и т.д.)  
✅ **Backend код** - готов к работе  
✅ **Frontend** - собран и готов  

**Ничего дополнительно устанавливать не нужно!**

## Результат

После сборки вы получите:

1. **Артефакт в Actions** - можно скачать ZIP архив
2. **Релиз в GitHub** (если запущено через тег) - с прикрепленным архивом

### Структура архива

**macOS:**
```
FeedChecker-macOS-portable-v1.0.0.zip
└── FeedChecker.app/
    └── Contents/
        ├── MacOS/
        │   └── FeedChecker
        └── Resources/
            ├── python/          # Python 3.11 + все зависимости
            └── backend/          # Backend код
```

**Windows:**
```
FeedChecker-Windows-portable-v1.0.0.zip
└── FeedChecker.exe
└── resources/
    ├── python/                  # Python 3.11 + все зависимости
    └── backend/                 # Backend код
```

## Размер

Примерно **200-300 MB** (включая Python и все зависимости)

## Требования для сборки

GitHub Actions автоматически:
- ✅ Устанавливает Node.js 18
- ✅ Устанавливает Python 3.11
- ✅ Кэширует зависимости для ускорения
- ✅ Собирает все компоненты

**Вам ничего устанавливать не нужно!**

## Примеры использования

### Создание релиза v1.0.0

```bash
# 1. Создайте тег
git tag v1.0.0

# 2. Запушьте тег
git push origin v1.0.0

# 3. GitHub Actions автоматически:
#    - Соберет версию
#    - Создаст релиз
#    - Прикрепит архивы
```

### Ручная сборка для тестирования

1. Откройте **Actions** → **Build macOS Portable**
2. Нажмите **Run workflow**
3. Дождитесь завершения
4. Скачайте артефакт

## Отладка

Если сборка не удалась:

1. **Проверьте логи** в разделе Actions
2. **Убедитесь**, что все файлы закоммичены:
   - `build-portable.sh` (для macOS)
   - `build-portable.ps1` (для Windows)
   - `package.json`
   - `backend/requirements.txt`
3. **Проверьте**, что скрипты исполняемые (для macOS)

## Примечания

- ⏱️ Сборка занимает **10-20 минут** (зависит от размера зависимостей)
- 💾 Артефакты хранятся **30 дней**
- 🔄 Для долгосрочного хранения используйте **релизы GitHub**
- 🎯 Для создания релизов нужен тег формата `v*` (например, `v1.0.0`)

## Что дальше?

1. **Закоммитьте** workflow файлы в репозиторий
2. **Запушьте** в GitHub
3. **Проверьте** в разделе Actions, что workflows появились
4. **Запустите** сборку вручную для тестирования
5. **Создайте тег** для автоматического релиза

Готово! Теперь у вас есть автоматическая сборка портативных версий! 🎉

