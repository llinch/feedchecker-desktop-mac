# Конфигурация окружений для FeedChecker Frontend

## 📋 Обзор

Проект использует разные API URL для разных окружений:

| Окружение | Файл | API URL | Использование |
|-----------|------|---------|---------------|
| **Локальная разработка** | `.env.development` | `http://localhost:8000` | `npm run dev` |
| **Локальная разработка (custom)** | `.env.local` | `http://localhost:8000` | Переопределяет .env.development |
| **Production** | `.env.production` | *(пусто)* | `npm run build`, CI/CD |

## 🚀 Локальная разработка

### Запуск фронтенда в dev режиме:
```bash
cd apps/frontend
npm run dev
```

Фронтенд будет на `http://localhost:5173`
Запросы идут на `http://localhost:8000` (из `.env.development`)

### Запуск бэкенда:
```bash
cd apps/backend
uvicorn app.main:app --reload
```

Бэкенд будет на `http://localhost:8000`

## 🏭 Production (на сервере)

### Как это работает:

1. **Nginx** слушает на порту 80/443
2. **Фронтенд** отдается nginx как статика из `/var/www/html`
3. **Бэкенд** работает в Docker на порту 8000
4. **Nginx проксирует** все запросы `/api/*` на `backend:8000`

### Файл .env.production:
```bash
# Пустой URL = относительные запросы
VITE_API_URL=
```

### Запросы в production:
- Фронтенд делает запрос: `/api/check-feed`
- Nginx проксирует на: `http://backend:8000/api/check-feed`

### CI/CD в GitLab:
```yaml
variables:
  BACKEND_URL: ""  # Пустой для nginx proxy
```

## 🔧 Кастомизация

Если нужен другой URL для локальной разработки, создайте `.env.local`:
```bash
echo "VITE_API_URL=http://192.168.1.100:8000" > .env.local
```

`.env.local` не коммитится в git и переопределяет `.env.development`

## ⚠️ Важно!

- **НЕ коммитьте** `.env.local` в git
- **КОММИТЬТЕ** `.env.production` и `.env.development`
- В production `VITE_API_URL` должен быть **пустым**
