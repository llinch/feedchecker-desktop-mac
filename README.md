# FeedChecker Desktop

Десктопное приложение для проверки фидов на Windows.

## 🚀 Быстрый старт

### 1. Первая настройка (один раз)

```powershell
# Скопируйте файлы из any-feedchecker
.\copy-files.ps1

# Установите зависимости Electron
npm install

# Установите зависимости фронтенда
cd renderer
npm install
cd ..

# Установите зависимости Python бэкенда
cd backend
pip install -r requirements.txt
cd ..
```

### 2. Запуск приложения

```powershell
npm run dev
```

Это запустит:
- ✅ Electron окно приложения
- ✅ Vite dev server для фронтенда (порт 5173)
- ✅ Python бэкенд (порт 8000)

### 3. Сборка установщика

```powershell
npm run build
```

Установщик будет в папке `dist/` - `FeedChecker Setup x.x.x.exe`

## 📁 Структура проекта

```
feedchecker-desktop/
├── main/              # Electron main process (Node.js)
│   ├── index.js      # Главный процесс - управляет окнами и бэкендом
│   └── preload.js    # Preload скрипт для безопасного IPC
├── renderer/          # React фронтенд (Vite)
│   └── src/          # Исходники фронтенда (скопированы из any-feedchecker)
├── backend/           # Python бэкенд (FastAPI)
│   └── app/          # Исходники бэкенда (скопированы из any-feedchecker)
├── assets/           # Иконки и ресурсы
├── dist/             # Собранное приложение (после npm run build)
└── package.json      # Конфигурация Electron
```

## 🔧 Требования

- **Windows 10/11**
- **Node.js 18+** ([скачать](https://nodejs.org/))
- **Python 3.11+** ([скачать](https://www.python.org/downloads/))

## 📝 Как это работает

1. **Electron** создает окно приложения
2. **Vite** собирает React фронтенд
3. **Python бэкенд** запускается автоматически как дочерний процесс
4. Фронтенд общается с бэкендом через `http://127.0.0.1:8000`

## 🛠️ Разработка

### Обновление файлов из any-feedchecker

Если вы изменили что-то в `any-feedchecker`, обновите файлы:

```powershell
.\copy-files.ps1
```

### Изменение порта бэкенда

Отредактируйте `main/index.js`:

```javascript
const BACKEND_PORT = 8001; // измените на нужный порт
```

### Добавление иконки

Поместите иконку в `assets/icon.png` (256x256 или 512x512 PNG)

## 📦 Сборка для распространения

```powershell
# Сборка фронтенда
npm run build:renderer

# Сборка Electron приложения
npm run build
```

Результат:
- `dist/FeedChecker Setup x.x.x.exe` - установщик
- `dist/win-unpacked/` - распакованная версия

## ⚠️ Примечания

- В режиме разработки используется системный Python
- Для production можно встроить Python через PyInstaller (требует дополнительной настройки)
- Бэкенд автоматически запускается и останавливается с приложением
- Логи бэкенда можно посмотреть в консоли Electron (DevTools)

## 🐛 Решение проблем

### Бэкенд не запускается

Проверьте, что Python установлен:
```powershell
python --version
```

### Порт 8000 занят

Измените порт в `main/index.js` или остановите процесс, использующий порт.

### Ошибки при сборке

Убедитесь, что все зависимости установлены:
```powershell
npm install
cd renderer && npm install && cd ..
cd backend && pip install -r requirements.txt && cd ..
```

## 📄 Лицензия

То же, что и основной проект FeedChecker.
