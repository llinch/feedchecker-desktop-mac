# FeedChecker Desktop - Описание приложения

## 📋 Краткое описание

**FeedChecker Desktop** — десктопное приложение для Windows, предназначенное для проверки и валидации XML/YML фидов товаров. Приложение помогает выявлять ошибки в фидах перед их загрузкой в систему Diginetica.

## 🏗️ Архитектура

Приложение состоит из трех основных компонентов:

### 1. **Electron (Main Process)**
- **Технология:** Node.js + Electron
- **Файлы:** `main/index.js`, `main/preload.js`
- **Роль:**
  - Управление окнами приложения
  - Запуск и остановка Python бэкенда
  - Связь между фронтендом и бэкендом через IPC
  - Упаковка всего в единое десктопное приложение

### 2. **Frontend (Renderer Process)**
- **Технология:** React + TypeScript + Vite
- **UI библиотека:** shadcn/ui (Tailwind CSS)
- **Файлы:** `renderer/src/`
- **Роль:**
  - Пользовательский интерфейс
  - Формы для загрузки фидов (URL или файл)
  - Отображение результатов проверки
  - Визуализация ошибок и рекомендаций

### 3. **Backend (Python API)**
- **Технология:** Python 3.11+ + FastAPI + Uvicorn
- **Файлы:** `backend/app/`
- **Роль:**
  - Парсинг XML/YML фидов
  - Валидация структуры фидов
  - Проверка обязательных полей
  - Поиск ошибок и проблем
  - Генерация отчетов в Excel
  - Обработка дельта-фидов (CSV)

## 🔄 Как это работает

1. **Запуск приложения:**
   - Electron создает окно приложения
   - Автоматически запускается Python бэкенд на порту 8000
   - Загружается React интерфейс

2. **Проверка фида:**
   - Пользователь вводит URL фида или загружает файл
   - Фронтенд отправляет запрос на бэкенд через API
   - Бэкенд загружает и парсит фид
   - Выполняется валидация и проверка на ошибки
   - Результаты возвращаются фронтенду
   - Пользователь видит детальный отчет с ошибками

3. **Типы проверок:**
   - Синтаксическая проверка XML
   - Проверка обязательных полей (id, name, price, available и т.д.)
   - Валидация категорий
   - Проверка дубликатов
   - Анализ параметров и атрибутов
   - Проверка дельта-фидов (CSV формат)

## 💻 Технологический стек

### Frontend:
- **React 18** — UI библиотека
- **TypeScript** — типизация
- **Vite** — сборщик и dev server
- **Tailwind CSS** — стилизация
- **shadcn/ui** — компоненты UI
- **React Router** — навигация (HashRouter для Electron)
- **Lucide React** — иконки

### Backend:
- **Python 3.11+** — язык программирования
- **FastAPI** — веб-фреймворк для API
- **Uvicorn** — ASGI сервер
- **lxml** — парсинг XML
- **pandas** — обработка данных (для дельта-фидов)
- **openpyxl** — генерация Excel отчетов
- **pymorphy3** — морфологический анализ
- **treelib** — работа с деревьями категорий

### Desktop:
- **Electron 28** — фреймворк для десктопных приложений
- **electron-builder** — сборка установщика
- **Node.js** — runtime для Electron

## 📦 Требования для работы

### Для конечного пользователя (установленное приложение):
- ✅ **Windows 10/11** (64-bit)
- ✅ **Python 3.11+** установлен в системе
- ✅ Python доступен через PATH (команда `python` работает в командной строке)

**Примечание:** Приложение автоматически использует системный Python для запуска бэкенда.

### Для разработчика:
- **Node.js 18+** — для сборки и разработки
- **Python 3.11+** — для бэкенда
- **npm** — менеджер пакетов Node.js
- **pip** — менеджер пакетов Python

## 📁 Структура проекта

```
feedchecker-desktop/
├── main/              # Electron main process
│   ├── index.js       # Управление окнами и бэкендом
│   └── preload.js     # Безопасный IPC мост
├── renderer/          # React фронтенд
│   ├── src/
│   │   ├── pages/     # Страницы приложения
│   │   ├── components/# UI компоненты
│   │   └── services/  # API клиент
│   └── vite.config.ts
├── backend/           # Python бэкенд
│   └── app/
│       ├── main.py    # FastAPI приложение
│       ├── feed_checker.py      # Проверка XML фидов
│       └── delta_feed_checker.py # Проверка дельта-фидов
└── dist/              # Собранное приложение
    └── FeedChecker Setup 1.0.0.exe
```

## 🚀 Процесс сборки

1. **Сборка фронтенда:**
   ```bash
   cd renderer
   npm run build
   ```
   Создает `renderer/dist/` с оптимизированными файлами

2. **Сборка Electron приложения:**
   ```bash
   npm run build
   ```
   Создает установщик `dist/FeedChecker Setup 1.0.0.exe`

3. **Результат:**
   - Установщик Windows (NSIS)
   - Все зависимости упакованы
   - Python бэкенд включен в `resources/backend/`

## 🔌 API Endpoints

Бэкенд предоставляет REST API на `http://127.0.0.1:8000`:

- `POST /api/check-feed` — полная проверка фида
- `POST /api/check-feed-async` — асинхронная проверка (для больших файлов)
- `GET /api/job/{job_id}` — статус асинхронной задачи
- `POST /api/check-syntax` — только синтаксическая проверка
- `GET /health` — проверка работоспособности
- `GET /api/logs` — получение логов бэкенда

## 📝 Особенности

- ✅ **Автоматический запуск бэкенда** — Python сервер запускается вместе с приложением
- ✅ **Асинхронная обработка** — поддержка больших фидов через job queue
- ✅ **Прогресс загрузки** — отображение прогресса при загрузке больших файлов
- ✅ **Экспорт в Excel** — генерация детальных отчетов
- ✅ **Поддержка дельта-фидов** — проверка CSV формата
- ✅ **Детальная валидация** — проверка всех аспектов фида
- ✅ **Обратная связь** — встроенный виджет для отзывов

## 🎯 Назначение

Приложение предназначено для:
- Проверки фидов перед загрузкой в Diginetica
- Выявления ошибок в структуре данных
- Валидации обязательных полей
- Анализа качества данных
- Генерации отчетов для исправления ошибок

